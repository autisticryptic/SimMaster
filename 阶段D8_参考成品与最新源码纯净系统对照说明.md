# 阶段 D8：SimAdmin-VoLTE 与最新源码纯净系统真机对照说明

> 测试日期：2026-07-18（Asia/Shanghai）  
> 目标设备：高通 410，Debian 13，Linux 6.17.0-rc6，ModemManager 1.24.0，libqmi 1.36.0  
> 运营商：中国电信，MCC/MNC 46011  
> 结论范围：本文件只描述本轮冷启动真机结果，不覆盖历史成功事实。

## 1. 对照对象

| 项目 | 参考成品 | 最新源码候选 |
|---|---|---|
| 版本/提交 | `1.1.6-dev18 / 05ea96a` | `1.1.3 / 0979f33` |
| 来源 | `SimAdmin-VoLTE/simadmin` | 当前 `SimAdmin` 分支交叉编译 |
| 大小 | 8,341,216 bytes | 9,536,680 bytes |
| SHA-256 | `D5FA0CC55ACD63339DE775137897DEE7ECD27C82EF33B419A8B1D4D2932EDA1B` | `E7C020FFBFF76AFC63E578562ABF3897CC25C818CB93663F46B2513B879CBBF2` |
| 部署目录 | `/opt/simadmin/releases/reference-05ea96a-clean` | `/opt/simadmin/releases/0979f33-unlimited` |
| 普通蜂窝数据 | 关闭 | 关闭 |
| VoLTE | 开启 | 开启 |

`0979f33` 去除了 VoLTE 自动恢复的单批次数限制：只要 VoLTE 功能和连接开关保持开启，就持续重试；每次重试前重新读取配置，失败后按配置延迟，不在等待期间占用连接锁。

## 2. 统一测试基线

设备恢复 Debian 初始状态并冷启动后，清理得到以下共同基线：

```text
LTE registration: home
packet service: attached
CID 1: IPV4V6, active
CID 2: absent
ModemManager user bearers: none
wwan0..wwan7: DOWN, no address
WDS packet service: disconnected
XFRM state/policy: 0/0
SimAdmin process: none
```

两个候选均使用隔离的配置、数据库、日志和 `127.0.0.1:3105`，没有使用正式服务配置。

## 3. 最新源码 `0979f33` 实测

### 3.1 已验证行为

1. 能识别稳定 Modem/SIM 线路，并在 Modem 对象编号变化后重新绑定。
2. 固定执行双栈优先、明确拒绝后按单栈回退的地址族策略。
3. IMS context 未取得 P-CSCF 时在 bearer 前停止，不继续构造虚假的路由、XFRM 或 REGISTER 状态。
4. 无限持续重试生效，日志已观察到 `reconnect_attempt=1..10`，没有在第 3 次停止。

### 3.2 实际失败路径

前 5 次重试曾把 CID 2 激活为 IPv4：

```text
CGDCONT: 2,"IP","ims"
CGACT: 2,1
CGCONTRDP=2: no P-CSCF
error: volte_runtime_all_pcscf_failed:AT+CGCONTRDP=2:IP:no-pcscf
```

后续重试中 ModemManager Modem 对象消失并重枚举，CID 2 回到纯 IPv6但未激活：

```text
CGDCONT: 2,"IPV6","ims"
CGACT: 2,0
CGPADDR=2: 0.0.0.0
CGCONTRDP=2: empty
AT+CGACT=1,2: No network service
```

持续重试期间出现 packet service `attached/detached` 波动、ModemManager 重启和设备整机重启。整机重启发生在连续重试测试窗口内，但本轮没有足够证据断言唯一原因；可以确认高频 IMS context 激活会显著放大当前基带不稳定状态。

## 4. 参考成品 `05ea96a` 实测

### 4.1 启动阶段

参考成品启动后会自行写入 ModemManager debug override 并重启服务，同时启动：

```text
Native VoLTE SMS runtime supervisor enabled
```

第一次 ModemManager 重启后设备没有重新枚举 Modem。参考成品将普通数据 watchdog 暂停为 `VoLTE IMS SMS path enabled`，不会主动重启 ModemManager，因此连续两轮以以下错误结束：

```text
volte_runtime_mm_modem_present_timeout:state=unknown,packet=unknown
```

手动重启一次 ModemManager 后恢复 `home/attached`，参考成品继续运行，不需要重启 SimAdmin 进程。

### 4.2 稳定 Modem 后的两个完整周期

两个周期都成功完成：

```text
ModemManager modem is present
ModemManager modem is ready for VoLTE IMS bearer
IMSI prefix 46011 loaded
IMS domain ims.mnc011.mcc460.3gppnetwork.org loaded
SMSC loaded
```

随后参考成品创建 CID 2 为 `IPV6,"ims"`，每周期执行三轮 P-CSCF 初始化，但三轮 `CGACT` 均失败：

```text
Native VoLTE runtime MM P-CSCF discovery CGACT command failed
GDBus.Error: org.freedesktop.ModemManager1.Error.MobileEquipment.Unknown
cycle=1,2,3
```

每周期结束后参考成品执行 `Cleaning up native VoLTE IMS context`。最终状态为：

```text
CID 2: IPV6,"ims", inactive
P-CSCF: none
ModemManager bearer: none
wwan address/route: none
XFRM state/policy: 0/0
IMS REGISTER: not started
```

## 5. 关键对照

| 阶段 | 参考成品 `05ea96a` | 最新源码 `0979f33` |
|---|---|---|
| debug 模式 | 自动安装 override 并重启 MM | 自动安装 override 并重启 MM |
| 首次 MM 丢失 | supervisor 等待，普通数据 watchdog 暂停 | watchdog 扫描并可重启 MM，支持对象重绑定 |
| 身份/SMSC | 成功 | 历史轮次成功；本轮重点在 PDP/P-CSCF |
| 地址族 | 固定 IPv6 CID 2 | 双栈首试，按拒绝结果回退 IPv4/IPv6 |
| 本轮最远阶段 | IPv6 `CGACT` | CID 2 曾激活，但无 P-CSCF |
| bearer | 未创建 | 门禁阻止创建 |
| IPv6 prefix | 未取得 | 直接纯 IPv6测试仍为 `prefix-unavailable` |
| XFRM/REGISTER | 未进入 | 未进入 |
| 自动恢复 | supervisor 周期重试，周期较长 | `0979f33` 无总次数上限，配置化延迟 |

## 6. 结论

1. **不能再表述为“参考成品当前一定能注册，而二改版本一定不能”。** 在本轮相同冷启动设备状态下，参考成品也无法激活 IMS CID 2。
2. **历史成功事实仍然成立。** 参考成品此前在同一设备完成过 IPv6 IMS bearer、P-CSCF、XFRM 和 REGISTER；`95f63ff` 历史日志也记录过 REGISTER 及 3 条 MT SMS/RP-ACK。
3. **当前公共阻塞位于 SIP 之前。** 两者都没有到达 bearer/REGISTER；本轮无法用 SIP、AKA、IPsec 或 SMS 实现差异解释失败。
4. **最新源码的门禁语义正确。** 没有地址/P-CSCF 时不应继续创建 bearer 或报告 registered。
5. **无限重试只证明恢复循环不会停止，不能修复 IMS context。** 当前高频重试还会造成 packet detach、Modem 重枚举和系统稳定性风险。

## 7. 下一步定位重点

下一轮不应继续盲目增加重试次数，而应捕获“参考成品成功时”与本轮失败时的状态差异：

1. 成功前后的 QMI WDS profile 2、PDP type、PCO、IMS flag 和 raw-ip/mux 数据路径状态。
2. 成功路径中 `shared_wwan_data` 的 `wwan1/a2-mux-rmnet1` 与 `wwan0/a2-mux-rmnet0` 探测顺序和副作用。
3. `CGDCONT/CGACT/CGCONTRDP` 前是否存在额外 QMI WDS start/stop、NAS attach、profile bind 或 link reset。
4. 冷启动后第一次 ModemManager debug 重启为什么经常无法重新枚举，以及何种条件下第二次重启可以恢复。
5. 保留无限恢复语义，但加入指数退避、连续 Modem 丢失熔断和整机稳定性保护。

本轮测试结束后已停止两个候选，删除 CID 2，清理 bearer/XFRM，并恢复到 `home/attached`、仅 CID 1、wwan DOWN、XFRM 0/0 的设备基线。测试目录与日志保留在设备上供后续取证。
