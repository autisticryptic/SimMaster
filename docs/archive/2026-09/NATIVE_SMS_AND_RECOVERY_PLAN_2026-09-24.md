# Native 短信与代次恢复接续计划（2026-09-24）

> 历史档案：本文件保留对应日期的事实，旧版本、worktree 和操作步骤不代表当前状态。
> 当前接手请读 [HANDOFF](../../HANDOFF.md)，不要重放旧部署、回滚或设备命令。

接续 `2026-09-19T.jsonl` 的原待办 #26；基线 `ac36dff`。既有未提交 PDU/收件箱草稿
尚未接入业务或经过 Rust CI，不能视作完成。

## 约束

- MM 保持默认；native 仍需显式实验性启用，不自动切换 owner。
- 不在 SIM-04 执行 native 接管、USB/NV/重启等设备写操作，不恢复回滚或续期轮询。
- 本地仅格式/diff/Python 守卫；Rust 单元测试及双架构构建只在 GitHub Actions 执行。
- 原始 PDU、号码、SIM 身份不进入公共事件或提交记录；私有收件箱允许持久化恢复。
- 只有持久化成功才允许确认直接短信；不确定的 ACK 不重试，不用裸 TP-MR 判定送达。
- 代次变化不等于资源已消失；恢复需核实归属，不能猜 CID、自动删除不明 receipt。

## 工作项（代码、CI、实机分开记录）

- [x] 有界直接 PDU 队列、SIM/会话作用域、完整帧检查及安全 ACK。
- [x] 私有 SQLite 收件箱、原子去重/入库/事件、分片重组和故障重放。
- [x] 发送引用持久化，按线路/SIM/收件人/引用/时间严格关联送达报告。
- [x] 监听器/API/UI 接线与硬件无关回归。
- [x] 受控跨代次恢复入口及孤儿资源证据核验（仅归档已确认完整清理；未知资源仍拒绝）。
- [x] 两套 CI、amd64/arm64 构建通过，Publish Release 为 skipped。
- [x] 更新 native 审计及交接记录（`19c3c12`）。
- [ ] native 硬件短信/通话/代次故障实测（需独立设备与授权，不能由 MM 验收替代）。

## 检查点

- `a1be268`：短信链路两套回归、前端和双架构构建全部通过，发布 skipped；
  详见 [持久化短信收件箱](../../NATIVE_SMS_INBOX.md)。
- `a4a83c2`：恢复入口及 UIM/CID 生命周期；Validate `35989431881` / Build `35989431842` 通过。
- `302b70e`：持久短信重放与存储扫尾解耦、事务内槽位/换卡复核；
  Validate `36003900244` / Build `36003900240` 通过，Publish Release skipped；
  范围与限制见 [显式资源恢复](../../NATIVE_RESOURCE_RECOVERY.md)。
- 以上均为代码/CI 状态，不代表 native 实机或恢复实测通过。

## 后续主线：SIM-06 中国电信 IMS 注册失败

用户指定的下一项目标；用户已确认设备当前离线，先保存本地交接，待上线后再恢复只读访问，
不继续轮询。本阶段尚未取得任何 SIM-06 现场数据，**不得复用 SIM-04/05 的失败阶段、
P-CSCF/APN 结论或 identity 判别**。

2026-09-25T02:30:39Z、2026-09-26T02:06:17Z 与本轮 2026-09-26T02:20:30Z、02:44:53Z
四次只读尝试均在 Cloudflare WebSocket 阶段失败：HTTP 530 / Cloudflare 错误码 1033，无 Access 登录跳转。
这说明 Tunnel/connector 当前不可达，
请求未到 SSH，**既不构成 SIM-06 注册失败证据，也不能据此认定 Cookie 过期**。
需要设备侧隧道或网络操作者恢复入口；恢复前只能做离线审查。

现场恢复后的**只读**顺序（默认 MM、不改 owner、不写 NV/USB/重启、不自动回滚）：

1. 读实际运行版本、commit、arch、服务状态与启动方式，确认是否为本轮构建；不是也先记录再决定。
2. 读线路归属、SIM 作用域与后端模式，确认该卡对应的 `line_id`；不切换 owner。
3. 取 IMS `phase`/`stage`/`last_error`、重试与退避时间、是否有未解决 receipt。
4. 定位失败步：Bearer/IP 族、P-CSCF、AKA、SIP 响应码中的哪一步；无数据不宣称某一步。
5. 仅在证据指向具体缺陷后才改动，改动经 CI 后单独授权部署，并以独立方法验证。

只读采证规范脚本为 [`scripts/ims-readonly-evidence.sh`](../../../scripts/ims-readonly-evidence.sh)。
2026-09-26 已补齐原私有脚本遗漏的初始 REGISTER、AKA 挑战、终止响应和 restore 失败，
区分安装信息与实际运行程序哈希；新工具 28 项回归、全套 157 项 Python 守卫通过。
这不是 SIM-06 的现场证据或修复；日志归属仍需与线路 API 私下交叉核对。
详细使用方式、约束与解除隧道阻塞所需动作见
[SIM-06 排查接续](../../IMS_DIAGNOSTICS.md)。
取得现场数据后在本文件追加脱敏结论，而不是把假设写成已验收。
