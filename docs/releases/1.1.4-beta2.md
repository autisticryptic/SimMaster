# 1.1.4-beta2：蜂窝 IMS 命名与纯 Rust DNS

状态：**最新候选 48e37fc 已通过最终回归、双架构构建并发布/部署；包含 IMS 命名、Hickory DNS、自动/单注册设置及 VoWiFi 资费门禁。配置/API 和本候选原通道自然续期验收通过；当前网络未接受多流，按设计单注册回退，不宣称实机双注册成功。**

## 变更

- 蜂窝 IMS 实现模块改为 `cellular_ims`，相应类型、运行时成员、函数、前端组件及代码变量使用准确名称。
- 蜂窝和 WLAN 共用的 profile 类型使用 `ImsProfile*`；真实 VoLTE 标准/运营商字段不做全局字符串替换。
- 新增 `/api/cellular-ims/lines...` 和线路级 `cellular-ims/call/status`；旧 `/volte/...` 端点仍兼容。
- 保留既有 JSON、数据库键、`volte` / `volte_ims` 传输标识及错误码作为兼容编码；新增 `cellular_ims_*` 等读取别名，拒绝重复别名。不会强制改写历史短信、自动化或用户配置。
- 普通系统解析改用 `hickory-resolver 0.25.2`，启用 `tokio`、`system-config`；不再调用 libc `lookup_host`。HTTP 和 SOCKS5 代理端点也使用纯 Rust 解析入口。
- 数字地址不发 DNS；hosts 优先于系统 resolver 配置；每次解析拥有独立有界 resolver，不共享跨 runtime/namespace 的缓存和 socket。
- 新 resolver 使用 `UserProvidedOrder` 保持系统 DNS 顺序，避免随机初始 RTT 让不可达后续服务器先耗尽查询预算；不覆盖系统 timeout/attempts，不修改设备 DNS。
- 专用运营商 DNS、P-CSCF、NAPTR、SOCKS5 UDP DNS 维持既有 Rust 传输和选路策略，不借重构改变出口。
- 补全 SIM DNS 覆盖校验，拒绝非法服务器地址和目标端口 0。
- 修正精简 YAML 补默认子树时的节点插入问题；保留原注释和未修改文本，写入前仍重新解析验证。新增复杂子树使用合法 flow YAML，首次生成配置仍是带注释的块式 YAML。

## 已有验证

- 开发分支 `e6f7b0d`，Actions run `34168332351`：编译、DNS、配置/别名、SIM override、IMS、私有 D-Bus 新旧 HTTP 契约及前端检查通过。
- 无本机后端编译；本地只做格式、前端/Python 检查和 Cargo 依赖/锁文件解析。
- 最终 Build-Release 同样包含新回归门禁，并拒绝空测试过滤器；不以开发分支的成功替代最终发布构建。
- 最终候选 `2bb6099` / run `34169025117`：前端、完整回归、私有 D-Bus API、ARM64、AMD64、发布全部成功，pre-release / 非 latest，tag/包/设备 commit 一致。
- 2026-09-08 07:34 部署到 410；07:35:32 VoWiFi REGISTER 200，实际租期 3553 秒。初始 494/403 响应驱动的头变体协商属于初始注册，不是 refresh。
- 07:40/07:52 新旧 API、profile-selection、call/status 和 8 个未认证 GET 的 401 检查通过；持久化线路配置和 config.yaml 与升级前备份一致；前端入口 HTML/JS 与部署文件一致。
- 首个候选 2bb6099 在 08:25:52 完成原通道自然 refresh：CSeq=3、Security-Verify、发送时剩余 532 秒，0.36 秒收到 200、续得 2858 秒，服务/TUN 未更换，`reused_access=true`。

## 实机发现并修正的 DNS 问题

- 普通 HTTP DNS 查询实际选中了不可达的后续系统 DNS，4 秒内未收到响应；独立 UDP 诊断显示配置首台服务器可用。不能把该诊断冒充应用路径成功。
- 根因是每次新建 Hickory resolver 时，默认 `QueryStatistics` 随机初始化 RTT 并重排服务器。修正 `0b97b4f` 新增六服务器/重复新建 resolver 回归，通过分支 CI `34172883931` 和最终 Build-Release `34173433980`。
- 08:28 有条件、原子地将 master 与 beta2 标签前移到修正版；旧候选和校验证据保留。08:43 校验产物后部署，仍为 pre-release、非 latest，不生成新版本号。
- 08:54:46 应用向配置首台 DNS 发出 A/AAAA 查询，分别得到 A 地址和有效 AAAA NODATA；HTTPS 元数据读取 0.496 秒成功。没有改 resolv.conf、触发 OTA 安装或用独立脚本替代 Rust 应用解析。
- 修正版新旧 API、鉴权、完整线路配置及 YAML、前端入口文件再次检查通过。08:44:38 初始注册租期 3342 秒、refresh_after=2742 秒。
- **修正版自身自然续期已通过**：09:31:28 发送 CSeq=3、Security-Verify 的 REGISTER，真实剩余租期 531 秒；完整脱敏元数据确认同事务 0.292738 秒后 200、续得 3100 秒。`reused_access=true`，服务进程/TUN index 未变，已到计划续期时点且仍在有效租期内；不是重建安全关联重新注册。
- 09:37 最终检查为 0b97b4f、active、0 通话、配置未变；09:43 只停止本次临时元数据抓包，保留所有证据和备份，没有重启主服务。

## 重要限制

- 本次不修改 beta1 的绑定所有权、自然续期、安全上下文和业务优先级，不恢复固定 120 秒测试间隔。
- beta1 在当前 410 上已验证优先级回退和原通道自然续期；两路当前网络响应均未接受 outbound。**不能把这个限制改写为实机双注册成功。**
- 当前 beta2 仅 WLAN 注册，未重做蜂窝优先对照；不能把 beta1 单路验证冒充 beta2 双路续期。
- Trunk 未启用、真实短信/电话、补充业务、浏览器交互未实测；不擅自开启或发送。前端资源 HTTP 检查不是浏览器交互验收。
- 回滚备份：`/opt/simadmin/manual-backup/20260908-084315-beta2-0b97b4f` 含首个 beta2/2bb6099（有已知 DNS 排序问题）；`/opt/simadmin/manual-backup/20260908-073410-beta2-2bb6099` 含 beta1/e55780a。回滚前检查通话并备份当前数据，默认保留兼容的现有配置/数据库，避免覆盖新短信和历史记录。
- 包哈希、日志与后续自然续期实测以 `plan.md` 为准；未验证项不标记完成。同版本文档更新不能无意触发 master 发布并覆盖已核验资产。

## 自动注册模式与资费门禁（48e37fc）

- TS 24.229 V18.11.0 明确允许协商成功后的多 IMS 流；保留实现，默认声明 outbound，
  未接受时走单注册，而不是强行追加或重建安全关联。中文依据见 `docs/IMS_REGISTRATION_POLICY.md`。
- 新增线路 IMS 页“自动双注册 / 仅单注册”设置，复用兼容偏好接口，不改变启用意图或资费开关。
- 第二路被拒绝/协商校验失败时，单注册回退仍优先 VoWiFi；不提前撤销健康主流或中断通话。
- 呼出在两路可用时固定 VoWiFi 优先；短信/Trunk 的 VoWiFi-only 是硬限制，覆盖缓存回退、
  来电提前接听、振铃后设置变更和 API/自动化的 CS 旁路；接收短信允许多路并去重。
- 后端候选 `2de7d85` / Actions `34193792866` 通过；最终 `48e37fc` 又分别通过
  独立 CI `34196362725` 和 Build-Release `34197531701`，包含 4 项前端单元、
  额外流拒绝、两种 IMS 的模拟 SIP 480、变更限制后的回退/接听和实际 HTTP 设置契约。
- 15:57 部署并保留 `/opt/simadmin/manual-backup/20260908-155656-beta2-48e37fc`，
  其中是上一版 0b97b4f。包 SHA256：
  `e25d3d49e7eb82bf49fd403226454f72bc73f72880ee937d7bcf8ef7ccaf45a2`；
  二进制 SHA256：
  `e5e6828d292d988ae8e2db87089cd05bd58762a1b1350c0b1ab4132dde235fe7`。
- 16:23 实机验证自动 → 单注册 → 自动并恢复原值；其他整份配置、服务和 TUN 未变，
  没有重连或发送电话/短信。应用 DNS/API/鉴权和前端入口资源复查通过。
- **16:47:58 本候选自然续期通过**：CSeq=3、Security-Verify、剩余 535 秒；
  同一事务 0.328795 秒后 200、续得 3115 秒，原服务/TUN、`reused_access=true`。
  17:18 最终检查为 active、0 通话、配置与升级前一致，临时观察已结束。
- 当前短信/Trunk 的“仅 VoWiFi”开关均关闭，Trunk 未启用；保留用户原状态。
  要强制阻止蜂窝/CS 业务回退需开启对应开关，不能将 VoWiFi 优先等同于仅 VoWiFi。
- 未做真实收费通话/短信或 Trunk/浏览器交互验收；回滚到 0b97b4f 会失去新增设置与资费门禁修正。

详情见 `docs/IMS_NAMING_MIGRATION.md`、`docs/DNS_HICKORY.md` 和 `plan.md`。
