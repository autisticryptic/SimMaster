# Native 短信与代次恢复接续计划（2026-09-24）

接续 `2026-09-19T.jsonl` 的原待办 #26；基线 `ac36dff`。既有未提交 PDU/收件箱草稿
尚未接入业务或经过 Rust CI，不能视作完成。

## 约束

- MM 保持默认；native 仍需显式实验性启用，不自动切换 owner。
- 不在 SIM-04 执行 native 接管、USB/NV/重启等设备写操作，不恢复回滚或续期轮询。
- 本地仅格式/diff/Python 守卫；Rust 单元测试及双架构构建只在 GitHub Actions 执行。
- 原始 PDU、号码、SIM 身份不进入公共事件或提交记录；私有收件箱允许持久化恢复。
- 只有持久化成功才允许确认直接短信；不确定的 ACK 不重试，不用裸 TP-MR 判定送达。
- 代次变化不等于资源已消失；恢复需核实归属，不能猜 CID、自动删除不明 receipt。

## 工作项（代码、CI、实机分开记录）

- [ ] 有界直接 PDU 队列、SIM/会话作用域、完整帧检查及安全 ACK。
- [ ] 私有 SQLite 收件箱、原子去重/入库/事件、分片重组和故障重放。
- [ ] 发送引用持久化，按线路/SIM/收件人/引用/时间严格关联送达报告。
- [ ] 监听器/API 接线与硬件无关回归。
- [ ] 受控跨代次恢复入口及孤儿资源证据核验。
- [ ] 两套 CI、amd64/arm64 构建通过，Publish Release 为 skipped。
- [ ] 更新 native 审计及交接记录。
- [ ] native 硬件短信/通话/代次故障实测（需独立设备与授权，不能由 MM 验收替代）。

## 后续主线顺序

用户在本次接续中确认：**SIM-04 自然续期已验证，SIM-05 已手动测试完成**。
不再把它们列为等待验收，也不重复采样；这不是 native 后端验收证据。

当前功能及 CI/文档收尾后，下一项目标是 **SIM-06 中国电信 IMS 注册失败**。
届时先收集脱敏的现场配置/注册阶段/日志，再定位与修复，不预设为 native 后端故障。
