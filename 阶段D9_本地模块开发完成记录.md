# 阶段 D9：本地可完成模块开发记录

日期：2026-07-19

本阶段把真实 SIM/Bearer 依赖之外的 Trunk、VoLTE 恢复、ViLTE 和诊断能力推进到可编译、可单测、可在 Web 中观察的状态。

## 1. VoLTE 恢复

- 每个自动恢复批次最多五次完整注册尝试。
- 每次 Bearer 尝试从 IPv4+IPv6 双栈开始。明确的 `Ipv4OnlyAllowed` / `Ipv6OnlyAllowed` 直接采用对应单栈；其他失败按 IPv4、IPv6 顺序回退。
- 仅复用明确标记为 `ipv4v6` 的旧 IMS Bearer，遗留单栈 Bearer 会先断开并删除。
- `/api/volte/lines/{line_id}/retry` 立即启动新的批次，不要求先关闭再打开 VoLTE。
- 基带长时间缺失时等待六次轮询，随后最多三次重启 ModemManager 并等待重新枚举；仍失败则暴露 `exhausted` 和手动重试状态。

## 2. Trunk runtime 诊断

每条线路的 runtime 现在提供本地/远端 SIP 端点、阶段、REGISTER 到期和退避、活跃对话/通话、SIP 收发帧/字节、INVITE/re-INVITE、媒体/视频协商、DTMF、Operator 事件、RTP Relay 包/字节和最近活动时间。Web 的“Trunk 诊断”页每 3 秒刷新这些数据。

## 3. re-INVITE 与 ViLTE

- IMS 运营商侧 re-INVITE 会转换为 Asterisk 侧 in-dialog INVITE。
- Asterisk 2xx 会生成 ACK 并提交 IMS 侧媒体变更；拒绝会原样返回，竞争事务使用 `491 Request Pending`。
- VoLTE 音频和 ViLTE 视频分别建立 RTP Relay，视频协商使用 H.264 SDP 和独立端口。
- ViLTE 总开关同步到每条线路的 OperatorLink，关闭时拒绝视频媒体协商，不影响音频通话。

## 4. Bruno 请求

新增 VoLTE 控制/线路/手动重试、ViLTE 控制、Trunk 配置/启用和 runtime 请求，并在 `bruno-api/README.md` 登记。

## 5. 验证与边界

- Rust 全量测试 556/556，VoLTE 专项 129 项通过，严格 Clippy 零告警。
- Trunk 专项及新增双向 re-INVITE 用例通过。
- 前端 TypeScript、ESLint、生产构建通过；桌面 1440x900 和移动 390x844 页面检查无重叠或横向溢出。
- 真实 IMS Bearer、REGISTER、Trunk 真机呼叫和短信收发待更换 SIM 后复验；本阶段没有修改安全加固策略。
